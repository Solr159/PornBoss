{
  "name": "javboss-cli",
  "private": true,
  "type": "module",
  "dependencies": {
    "inquirer": "^9.2.20"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  },
  "scripts": {
    "build": "esbuild cli.mjs --bundle --platform=node --format=cjs --outfile=build/javboss-cli.cjs"
  }
}
